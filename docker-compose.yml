services:
  frontend:
    container_name: sme-frontend
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:8000/api}
    ports:
      - "3000:3000"
    restart: unless-stopped
